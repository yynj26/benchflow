[project]
name = "benchflow"
version = "0.1.0"
description = "BenchFlow is a tool for easy benchmarking."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.3",
    "ruff>=0.9.2",
]
